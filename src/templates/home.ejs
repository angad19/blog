<!DOCTYPE html>
<html>
	<% let stylesheet = 'assets/style.css'; %>
	<% let feed = 'feed.xml'; %>
	<% include partials/head %>
	<body>
		<div class="container-fluid main">
			<% include partials/header %>
			<div class="posts list-group-flush">
				<% for(let i in posts) { %>
					<% include partials/post %>
				<% } %>
			</div>
			<% include partials/footer %>
		</div>
	</body>
</html>
